var numbers = [10, 20, 30, 5, 66, 2, 44];
var newNum = [];
for (var i = 0; i < numbers.length; i++) {
    var element = numbers[i];
    if (element > newNum.length) {
        //   newNum=element;
    }
}
var player;
player = {
    name: 'Smith',
    age: 34
};
console.log(player.age);
